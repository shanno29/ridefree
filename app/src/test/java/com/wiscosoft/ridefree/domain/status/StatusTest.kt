package com.wiscosoft.ridefree.domain.status

import org.junit.Assert.*
import org.junit.Test

//class StatusTest {
//
//  @Test
//  fun testStatusIntegerConverter() {
//    val converters = StatusConverters()
//
//    //assertEquals(Status.ACCEPTED, Status.valueOf("ACCEPTED"))
//
////    // status from integer
////    assertEquals(Status.UNKNOWN, converters.statusFromInt(-1))
////    assertEquals(Status.PENDING, converters.statusFromInt(0))
////    assertEquals(Status.ACCEPTED, converters.statusFromInt(1))
////    assertEquals(Status.IN_PROGRESS, converters.statusFromInt(2))
////    assertEquals(Status.COMPLETED, converters.statusFromInt(3))
////    assertEquals(Status.CANCELLED, converters.statusFromInt(4))
////    assertEquals(Status.UNKNOWN, converters.statusFromInt(5))
////
////    // integer from status
////    assertEquals(-1, converters.intFromStatus(Status.UNKNOWN))
////    assertEquals(0, converters.intFromStatus(Status.PENDING))
////    assertEquals(1, converters.intFromStatus(Status.ACCEPTED))
////    assertEquals(2, converters.intFromStatus(Status.IN_PROGRESS))
////    assertEquals(3, converters.intFromStatus(Status.COMPLETED))
////    assertEquals(4, converters.intFromStatus(Status.CANCELLED))
//  }
//
//}